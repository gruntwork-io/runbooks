# Deploy Module via GitHub PR

This runbook clones a GitHub repository, configures an OpenTofu or Terraform module,
generates a `terragrunt.hcl` file at a chosen path, and opens a pull request.

## Authenticate to GitHub

<GitHubAuth id="gh-auth" title="Connect to GitHub" />

## Clone Repository

<GitClone id="clone-repo" gitHubAuthId="gh-auth" title="Clone your infrastructure repo" />

## Choose Target Directory

<DirPicker id="target-path" gitCloneId="clone-repo" dirLabels={['Account', 'Region']} title="Select deployment path" pathLabelDescription="The path where your terragrunt.hcl file will be generated. Edit as needed." />

## Configure Module Variables

<TfModule id="module-vars" source="::cli_runbook_source" />

## Generated Configuration

<TemplateInline
  inputsId="module-vars"
  outputPath="{{ .outputs.target_path.PATH }}/{{ .outputs.module_vars.MODULE_NAME }}/terragrunt.hcl"
  generateFile={true}
  target="worktree"
>
```hcl
terraform {
  source = "{{ .inputs._module.source }}"
}

include "root" {
  path   = find_in_parent_folders("root.hcl")
  expose = true
}

inputs = {
{{- range $name, $hcl := .inputs._module.hcl_inputs_non_default }}
  {{ $name }} = {{ $hcl }}
{{- end }}
}
```
</TemplateInline>

## Create Pull Request

<GitHubPullRequest
  workspaceId="clone-repo"
  inputsId="module-vars"
  prefilledPullRequestTitle="Update {{ .outputs.module_vars.MODULE_NAME }} module"
  prefilledBranchName="feature/{{ .outputs.module_vars.MODULE_NAME }}"
  prefilledPullRequestDescription="## Changes\n\n- Added a new terragrunt.hcl file at {{ .outputs.target_path.PATH }}"
/>
