environment: {{ .inputs.environment }}
team_owner: {{ .inputs.team_owner }}

module:
  source: "{{ .inputs._module.source }}"

variables:
{{- range $name, $val := .inputs._module.inputs }}
  {{ $name }}: {{ $val }}
{{- end }}
