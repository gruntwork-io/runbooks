# Test configuration for demo2
# Generated by: runbooks test init
# Edit this file to customize your tests.

version: 1

settings:
  # Generate files to a temp directory (cleaned up after test)
  use_temp_output: true
  # Test timeout
  timeout: 5m
  # Can this runbook's tests run in parallel with others?
  parallelizable: true

tests:
  - name: happy-path
    description: Standard successful execution

    inputs:
      # GithubOrgName: string, required
      infrastructure-live-root-repo-inputs.GithubOrgName:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # GithubRepoName: string, required
      infrastructure-live-root-repo-inputs.GithubRepoName:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # Name: string, required
      test1.Name:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # OpenTofuVersion: string
      mise-config-inputs.OpenTofuVersion:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # TerragruntVersion: string, required
      mise-config-inputs.TerragruntVersion:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # AWSAccounts: map
      infra-live-elements-inputs.AWSAccounts:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # OrgNamePrefix: string
      infra-live-elements-inputs.OrgNamePrefix:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # DefaultRegion: enum [us-east-1, us-east-2, us-west-1, us-west-2, af-south-1, ap-east-1, ap-east-2, ap-south-1, ap-south-2, ap-southeast-1, ap-southeast-2, ap-southeast-3, ap-southeast-4, ap-southeast-5, ap-southeast-7, ap-northeast-1, ap-northeast-2, ap-northeast-3, ca-central-1, ca-west-1, eu-central-1, eu-central-2, eu-west-1, eu-west-2, eu-west-3, eu-south-1, eu-south-2, eu-north-1, il-central-1, mx-central-1, me-south-1, me-central-1, sa-east-1, us-gov-east-1, us-gov-west-1, cn-north-1, cn-northwest-1]
      infra-live-elements-inputs.DefaultRegion:
        fuzz:
          type: enum
          options: ["us-east-1", "us-east-2", "us-west-1", "us-west-2", "af-south-1", "ap-east-1", "ap-east-2", "ap-south-1", "ap-south-2", "ap-southeast-1", "ap-southeast-2", "ap-southeast-3", "ap-southeast-4", "ap-southeast-5", "ap-southeast-7", "ap-northeast-1", "ap-northeast-2", "ap-northeast-3", "ca-central-1", "ca-west-1", "eu-central-1", "eu-central-2", "eu-west-1", "eu-west-2", "eu-west-3", "eu-south-1", "eu-south-2", "eu-north-1", "il-central-1", "mx-central-1", "me-south-1", "me-central-1", "sa-east-1", "us-gov-east-1", "us-gov-west-1", "cn-north-1", "cn-northwest-1"]
      # DefaultTags: map
      infra-live-elements-inputs.DefaultTags:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # CatalogRepositories: list
      infra-live-elements-inputs.CatalogRepositories:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # RootTerragruntFileName: string
      infra-live-elements-inputs.RootTerragruntFileName:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20
      # AddAdditionalCommonVariables: bool
      infra-live-elements-inputs.AddAdditionalCommonVariables:
        fuzz:
          type: bool
      # CatalogTags: string
      infra-live-elements-inputs.CatalogTags:
        fuzz:
          type: string
          minLength: 5
          maxLength: 20

    steps:
      # Note: Order matters! Blocks that produce outputs must run before
      # blocks that consume them via {{ ._blocks.x.outputs.y }}
      # If no blocks are listed, the test runs all blocks in document order.

      - block: check-gh-install
        expect: success

      - block: install-gh-latest-version
        expect: success

      - block: check-mise-install
        expect: success

      # - block: create-infrastructure-live-root-repo
      #   expect: success

      # - block: check-infrastructure-live-root-repo
      #   expect: success

      - block: lookup-latest-versions-of-opentofu-and-terragrunt
        expect: success

      - block: test-command1
        expect: success

      - block: template-ci
        # Renders: ci.yml
        # Uses inputs from: infrastructure-live-root-repo-inputs
        expect: success

      # - block: create-pull-request-with-changes
      #   expect: success

    assertions:
      # TemplateInline blocks automatically validate their output dependencies.
      # Add additional assertions to validate test results:
      # - type: file_exists
      #   path: generated/README.md
      # - type: file_contains
      #   path: generated/README.md
      #   contains: "expected content"
      # - type: files_generated
      #   block: mise-config-inputs
      #   min_count: 1

    # cleanup:
    #   - command: rm -rf /tmp/test-resources
    #   - path: cleanup/teardown.sh
