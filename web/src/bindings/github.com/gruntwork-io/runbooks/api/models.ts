// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Create as $Create } from "@wailsio/runtime";

/**
 * BoilerplateConfig represents the parsed boilerplate.yml, which is a collection of variables
 */
export class BoilerplateConfig {
    "variables": BoilerplateVariable[];

    /**
     * Sections is an ordered list of section groupings for UI rendering.
     * Each Section contains a name and the list of variable names in that section.
     * Note: Individual variables also have a SectionName field for direct lookup.
     */
    "sections"?: Section[];

    /**
     * OutputDependencies lists references to other blocks' outputs found in template files.
     * These are {{ .outputs.blockId.outputName }} patterns that must be satisfied
     * before the template can be rendered. The Template component uses this to show warnings
     * when dependent blocks haven't been executed yet.
     */
    "outputDependencies"?: OutputDependency[];

    /** Creates a new BoilerplateConfig instance. */
    constructor($$source: Partial<BoilerplateConfig> = {}) {
        if (!("variables" in $$source)) {
            this["variables"] = [];
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new BoilerplateConfig instance from a string or object.
     */
    static createFrom($$source: any = {}): BoilerplateConfig {
        const $$createField0_0 = $$createType1;
        const $$createField1_0 = $$createType3;
        const $$createField2_0 = $$createType5;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("variables" in $$parsedSource) {
            $$parsedSource["variables"] = $$createField0_0($$parsedSource["variables"]);
        }
        if ("sections" in $$parsedSource) {
            $$parsedSource["sections"] = $$createField1_0($$parsedSource["sections"]);
        }
        if ("outputDependencies" in $$parsedSource) {
            $$parsedSource["outputDependencies"] = $$createField2_0($$parsedSource["outputDependencies"]);
        }
        return new BoilerplateConfig($$parsedSource as Partial<BoilerplateConfig>);
    }
}

/**
 * BoilerplateRequest represents the request body for boilerplate variable parsing
 */
export class BoilerplateRequest {
    /**
     * Path to the boilerplate template directory
     */
    "templatePath"?: string;

    /**
     * Direct content of the boilerplate.yml file
     */
    "boilerplateContent"?: string;

    /** Creates a new BoilerplateRequest instance. */
    constructor($$source: Partial<BoilerplateRequest> = {}) {

        Object.assign(this, $$source);
    }

    /**
     * Creates a new BoilerplateRequest instance from a string or object.
     */
    static createFrom($$source: any = {}): BoilerplateRequest {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new BoilerplateRequest($$parsedSource as Partial<BoilerplateRequest>);
    }
}

/**
 * BoilerplateValidationType represents the supported validation types from boilerplate
 * The list of supported validations is current as of Sep 2025.
 * https://github.com/gruntwork-io/boilerplate?tab=readme-ov-file#validations
 */
export enum BoilerplateValidationType {
    /**
     * The Go zero value for the underlying type of the enum.
     */
    $zero = "",

    ValidationRequired = "required",
    ValidationURL = "url",
    ValidationEmail = "email",
    ValidationAlpha = "alpha",
    ValidationDigit = "digit",
    ValidationAlphanumeric = "alphanumeric",
    ValidationCountryCode2 = "countrycode2",
    ValidationSemver = "semver",
    ValidationLength = "length",
    ValidationRegex = "regex",
    ValidationCustom = "custom",
};

/**
 * BoilerplateVarType represents the supported variable types from boilerplate.
 * These map to the boilerplate package's BoilerplateType values.
 */
export enum BoilerplateVarType {
    /**
     * The Go zero value for the underlying type of the enum.
     */
    $zero = "",

    VarTypeString = "string",
    VarTypeInt = "int",
    VarTypeFloat = "float",
    VarTypeBool = "bool",
    VarTypeList = "list",
    VarTypeMap = "map",
    VarTypeEnum = "enum",
};

/**
 * BoilerplateVariable represents a variable definition from boilerplate.yml
 * Note that we define a simplified version of the BoilerplateVariable struct here.
 * The official Variable struct in the boilerplate package is more complex and includes additional fields we don't need.
 */
export class BoilerplateVariable {
    "name": string;
    "description": string;
    "type": BoilerplateVarType;
    "default": any;
    "required": boolean;
    "options"?: string[];
    "validations"?: ValidationRule[];
    "sensitive"?: boolean;

    /**
     * Schema is a field not known to boilerplate itself, but we use it to allow users to define a schema for structured maps.
     * For example, a user could specify a schema for an AWS account map with the fields "email", "environment", and "id".
     * The frontend will then use this schema to render a form for the user to enter the data.
     * It will send the data to boilerplate as a map of strings, where the key is the entry name and the value is a map of strings,
     * where the key is the field name and the value is the field value.
     * YAML property: x-schema (Gruntbooks extension, ignored by Boilerplate)
     * For structured maps: field name -> type mapping
     */
    "schema"?: { [_ in string]?: string };

    /**
     * SchemaInstanceLabel is a field not known to boilerplate itself, but we use it to allow users to name an instance of a schema.
     * For example, a user could specify a schema instance label for an AWS account map with the value "Account Name".
     * The frontend will then use this label to render a form for the user to enter the data.
     * It will send the data to boilerplate as a map of strings, where the key is the entry name and the value is a map of strings,
     * where the key is the field name and the value is the field value.
     * YAML property: x-schema-instance-label (Gruntbooks extension, ignored by Boilerplate)
     * Custom label for schema instances (e.g., "Account Name")
     */
    "schemaInstanceLabel"?: string;

    /**
     * SectionName indicates which section this variable belongs to.
     * Used to look up a variable's section without searching the top-level Sections list.
     * See also: BoilerplateConfig.Sections for the ordered list of all section groupings.
     * YAML property: x-section (Gruntbooks extension, ignored by Boilerplate)
     */
    "sectionName"?: string;

    /** Creates a new BoilerplateVariable instance. */
    constructor($$source: Partial<BoilerplateVariable> = {}) {
        if (!("name" in $$source)) {
            this["name"] = "";
        }
        if (!("description" in $$source)) {
            this["description"] = "";
        }
        if (!("type" in $$source)) {
            this["type"] = BoilerplateVarType.$zero;
        }
        if (!("default" in $$source)) {
            this["default"] = null;
        }
        if (!("required" in $$source)) {
            this["required"] = false;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new BoilerplateVariable instance from a string or object.
     */
    static createFrom($$source: any = {}): BoilerplateVariable {
        const $$createField5_0 = $$createType6;
        const $$createField6_0 = $$createType8;
        const $$createField8_0 = $$createType9;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("options" in $$parsedSource) {
            $$parsedSource["options"] = $$createField5_0($$parsedSource["options"]);
        }
        if ("validations" in $$parsedSource) {
            $$parsedSource["validations"] = $$createField6_0($$parsedSource["validations"]);
        }
        if ("schema" in $$parsedSource) {
            $$parsedSource["schema"] = $$createField8_0($$parsedSource["schema"]);
        }
        return new BoilerplateVariable($$parsedSource as Partial<BoilerplateVariable>);
    }
}

/**
 * OutputDependency represents a reference to another block's output in a template.
 * These are found by scanning template files for {{ .outputs.blockId.outputName }} patterns.
 */
export class OutputDependency {
    /**
     * The block ID that produces the output (e.g., "create-account")
     */
    "blockId": string;

    /**
     * The output name (e.g., "account_id")
     */
    "outputName": string;

    /**
     * The full template reference (e.g., "outputs.create_account.account_id")
     */
    "fullPath": string;

    /** Creates a new OutputDependency instance. */
    constructor($$source: Partial<OutputDependency> = {}) {
        if (!("blockId" in $$source)) {
            this["blockId"] = "";
        }
        if (!("outputName" in $$source)) {
            this["outputName"] = "";
        }
        if (!("fullPath" in $$source)) {
            this["fullPath"] = "";
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new OutputDependency instance from a string or object.
     */
    static createFrom($$source: any = {}): OutputDependency {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new OutputDependency($$parsedSource as Partial<OutputDependency>);
    }
}

/**
 * Section represents a grouping of variables under a section header (Gruntbooks extension).
 * This is used for UI rendering (e.g., rendering groups of variables in sections in the form).
 * See also: BoilerplateVariable.SectionName for per-variable section lookup.
 * YAML property: x-section (Gruntbooks extension, ignored by Boilerplate)
 */
export class Section {
    /**
     * Section name ("" for unnamed/default section)
     */
    "name": string;

    /**
     * Variable names in this section (in declaration order)
     */
    "variables": string[];

    /** Creates a new Section instance. */
    constructor($$source: Partial<Section> = {}) {
        if (!("name" in $$source)) {
            this["name"] = "";
        }
        if (!("variables" in $$source)) {
            this["variables"] = [];
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Section instance from a string or object.
     */
    static createFrom($$source: any = {}): Section {
        const $$createField1_0 = $$createType6;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("variables" in $$parsedSource) {
            $$parsedSource["variables"] = $$createField1_0($$parsedSource["variables"]);
        }
        return new Section($$parsedSource as Partial<Section>);
    }
}

/**
 * ValidationRule represents a validation rule from a boilerplate.yml.
 */
export class ValidationRule {
    "type": BoilerplateValidationType;
    "message": string;
    "args"?: any[];

    /** Creates a new ValidationRule instance. */
    constructor($$source: Partial<ValidationRule> = {}) {
        if (!("type" in $$source)) {
            this["type"] = BoilerplateValidationType.$zero;
        }
        if (!("message" in $$source)) {
            this["message"] = "";
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new ValidationRule instance from a string or object.
     */
    static createFrom($$source: any = {}): ValidationRule {
        const $$createField2_0 = $$createType10;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("args" in $$parsedSource) {
            $$parsedSource["args"] = $$createField2_0($$parsedSource["args"]);
        }
        return new ValidationRule($$parsedSource as Partial<ValidationRule>);
    }
}

// Private type creation functions
const $$createType0 = BoilerplateVariable.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = Section.createFrom;
const $$createType3 = $Create.Array($$createType2);
const $$createType4 = OutputDependency.createFrom;
const $$createType5 = $Create.Array($$createType4);
const $$createType6 = $Create.Array($Create.Any);
const $$createType7 = ValidationRule.createFrom;
const $$createType8 = $Create.Array($$createType7);
const $$createType9 = $Create.Map($Create.Any, $Create.Any);
const $$createType10 = $Create.Array($Create.Any);
