import { GitLabAuth } from './GitLabAuth'

export { GitLabAuth }
export default GitLabAuth

// GitLabAuth is a thin alias of <GitAuth>; the types live in the GitAuth module.
export type { GitLabAuthProps } from '@/components/mdx/GitAuth/types'
